# ATAC API

Functions used by the ATAC pipeline:

1. [`preprocess_linux`](atac/preprocess_linux.md)
2. [`cell_types_linux`](rna/cell_types_linux.md) *(shared RNA/ATAC function)*
3. [`calculate_sample_embedding`](shared/calculate_sample_embedding.md) *(shared RNA/ATAC function; use `atac=True`)*
4. [`find_optimal_cell_resolution_linux`](shared/find_optimal_cell_resolution_linux.md) *(shared RNA/ATAC function; use `modality="atac"`)*

!!! note
    Shared functions are linked instead of duplicated.

